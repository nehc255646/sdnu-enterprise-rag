# Shared by backend1 / backend2 (must match)
JWT_SECRET=dev-shared-secret-change-me

# Optional LLM overrides (OpenAI-compatible; default = host Ollama)
OPENAI_API_KEY=sk-no-auth
OPENAI_MODEL=qwen2.5:1.5b
