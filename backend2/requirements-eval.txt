# Optional eval extras (also listed in requirements.txt for CI convenience)
# Pin langchain-community<0.4 so ragas can import ChatVertexAI shim path.
ragas>=0.2.15,<0.3
datasets>=2.14.0
langchain-community>=0.3.0,<0.4
