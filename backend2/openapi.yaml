openapi: 3.1.0
info:
  title: 山师大知识库问答 — Chat & Auth (backend2)
  version: 0.1.0
  description: >
    Canonical OpenAPI is served live at GET /openapi.json when the app runs.
    This file is a skeleton pointer for frontend and ingest-service integration.
    Qdrant collection: sdnu_chunks (must match ingest).
    Protected routes require Authorization: Bearer <JWT> and X-Tenant-Id
    (OpenAPI required=true; must match JWT tenant_id; empty→400, mismatch→403).
    Live schema: GET /openapi.json (Header(..., alias=X-Tenant-Id)).
    JWT handoff: docs/jwt-handoff.md (HS256, sub+tenant_id, JWT_SECRET).
servers:
  - url: http://localhost:8002
    description: local chat & auth (backend2)
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
    TenantHeader:
      type: apiKey
      in: header
      name: X-Tenant-Id
      description: Required on protected routes; must equal JWT tenant_id
security:
  - bearerAuth: []
    TenantHeader: []
paths:
  /api/v1/auth/register:
    post:
      tags: [auth]
      summary: Register user (optional tenant_id)
      security: []
  /api/v1/auth/login:
    post:
      tags: [auth]
      summary: Login → JWT access_token
      security: []
  /api/v1/auth/me:
    get:
      tags: [auth]
      summary: Current user (requires Bearer + X-Tenant-Id)
  /api/v1/sessions:
    get:
      tags: [sessions]
      summary: List sessions for current user/tenant
    post:
      tags: [sessions]
      summary: Create chat session
  /api/v1/chat:
    post:
      tags: [chat]
      summary: Non-streaming RAG chat
  /api/v1/chat/stream:
    post:
      tags: [chat]
      summary: SSE streaming RAG chat (token/citation/error/done)
  /api/v1/health:
    get:
      tags: [health]
      summary: Health + dependency status
      security: []
  /api/v1/llm/config:
    get:
      tags: [llm]
      summary: Read current OpenAI-compat LLM config (no api_key value)
    put:
      tags: [llm]
      summary: Hot-update base_url / api_key? / model; health reflects immediately
